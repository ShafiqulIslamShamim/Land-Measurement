<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Permissions -->
    <uses-permission
    android:name="android.permission.WRITE_EXTERNAL_STORAGE"
    android:maxSdkVersion="29"
  />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
    android:name=".LandMeasurementApp"
    android:usesCleartextTraffic="true"
    android:networkSecurityConfig="@xml/network_security_config"
    android:allowBackup="true"
    android:icon="@mipmap/ic_launcher"
    android:roundIcon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:supportsRtl="true"
    android:theme="@style/AppThemeDark"
    android:requestLegacyExternalStorage="true"
    android:enableOnBackInvokedCallback="true"
  >

        <activity
      android:name=".activity.SettingsActivity"
      android:exported="true"
      android:label=""
    />
    <activity android:name=".activity.IntroActivity" />
    <activity android:name=".activity.RectangularLandActivity" />
    <activity android:name=".activity.ScaleneLandActivity" />
    <activity android:name=".activity.CircularLandActivity" />
    <activity android:name=".activity.SquareLandActivity" />
    <activity android:name=".activity.ConversionActivity" />
    <activity android:name=".activity.TriangularLandActivity" />
    <activity android:name=".activity.QuadrilateralItemsActivity" />
        <activity android:name=".activity.MainActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
        android:name="android.app.shortcuts"
        android:resource="@xml/shortcuts"
      />
        </activity>
       
    </application>
</manifest>
